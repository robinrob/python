import sys
import random

MULTIPLIER = 1000
DEFAULT_LENGTH = 10

length = DEFAULT_LENGTH
if len(sys.argv) >= 2:
    length = int(sys.argv[1])


password = ""

for i in range(0, length):
    password += self.rand_num()
    

@staticmethod
def rand_num():
    return random.random() * MULTIPLIER

