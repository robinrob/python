import random

MULTIPLIER = 1000

BASE_ORD = 97

class Generator:
    
    
    def __init__(self):
        self.chars = []
        
        for i in range(0, 10):
            self.chars.append(i)
            
        for i in range(0, 26):
            self.chars.append(chr(61 + i))
            
        print(self.chars)
    
        
    def generate(self, length):
        password = ""
    
        for i in range(0, length):
            password += str(self.rand_char())
            
        return password
    
    
    def rand_char(self, max=9):
        return (int(random.random() * MULTIPLIER) % max) + 1